// import types

function validate() {
    
}

function createJson() {
    
}

function submit() {
    // validate the data and create a json file
    validate();
    createJson();
}

// on load even init fn and add event listener to the submit button